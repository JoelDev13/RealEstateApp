@model IEnumerable<RealEstateApp.Web.Models.Admin.Improvements.ImprovementViewModel>

@{
    ViewData["Title"] = "Mejoras";
}

<h2 class="mb-4">Mantenimiento de mejoras</h2>

<p>
    <a asp-action="Create" class="btn btn-primary">Crear nueva mejora</a>
</p>

<table class="table table-striped">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(m => m.First().Name)</th>
            <th>@Html.DisplayNameFor(m => m.First().Description)</th>
            <th>@Html.DisplayNameFor(m => m.First().IsActive)</th>
            <th class="text-end">Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Name</td>
                <td>@item.Description</td>
                <td>@(item.IsActive ? "Sí" : "No")</td>
                <td class="text-end">
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning me-1">
                        Editar
                    </a>

                    <form asp-action="ToggleStatus" asp-route-id="@item.Id" method="post" style="display:inline">
                        <button type="submit" class="btn btn-sm @(item.IsActive ? "btn-secondary" : "btn-success")">
                            @(item.IsActive ? "Desactivar" : "Activar")
                        </button>
                    </form>

                    <form asp-action="Delete" asp-route-id="@item.Id" method="post" style="display:inline"
                          onsubmit="return confirm('¿Seguro que deseas eliminar esta mejora?');">
                        <button type="submit" class="btn btn-sm btn-danger ms-1">
                            Eliminar
                        </button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
